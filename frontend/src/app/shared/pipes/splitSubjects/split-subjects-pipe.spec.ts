import { SplitSubjectsPipe } from './split-subjects-pipe';

describe('SplitSubjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitSubjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
